import type { NextPage } from 'next';

import ContactUsContainer from 'containers/contact';

const ContactUsPage: NextPage = () => {
  return <ContactUsContainer />;
};

export default ContactUsPage;
